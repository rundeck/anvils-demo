description: 'Given user in group "dev" and for job matching ".*/restart", then allow [read].'
for:
  job:
    - match:
        group: '.*'
        name: 'restart'
      allow: [read]
by:
  group: dev
